/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Uncomment for static export (Firebase Hosting)
  // output: 'export',
  // images: {
  //   unoptimized: true,
  // },
}

module.exports = nextConfig

